<source>
  @type  forward
  @label @raw
  port  24224
</source>

<label @raw>
  <match **>
    @type copy
    <store>
      @type relabel
      @label @main
    </store>
  </match>
</label>

<label @main>
  <match **>
    @type stdout
  </match>
</label>
